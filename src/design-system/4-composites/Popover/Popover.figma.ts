// source=src/design-system/4-composites/Popover/Popover.tsx
// component=Popover
//
// UNRESOLVED MAPPING — no valid Figma target.
// The previous `url=` targeted "[INK] Overlay" (node 980-13202), but that node is a
// modal DIALOG SHELL (header/footer/close/submit affordances), NOT a positioned
// popover-with-arrow. The code Popover's positioning props (position, align, showArrow)
// have no counterpart there. There is no popover / tooltip-bubble primitive in the
// library, so the Code Connect mapping is intentionally dropped (no `url=`) rather than
// pointed at a misleading node.
//
// DS FLAG: library is missing a Popover / tooltip-bubble component. Add one, then wire
// up this Code Connect file with the real node id.
export {}
